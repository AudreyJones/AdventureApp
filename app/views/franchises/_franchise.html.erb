require 'pry'</br>
<h1><%=@franchise.name%> - <%=@franchise.company%></h1>

<ol>
        <% if @franchise.adventures == [] %>
            Sorry, this franchise doesn't seem to have any adventures right now!
        <% else %>
            <% @franchise.adventures.each do |adventure|%>
                <li><%= adventure.name %> - <%= adventure.location %></li>
            <% end %>
        <% end %>
</ol>
<%= link_to("Back to Franchise Index", franchises_path)%><br/>
<%= link_to("Create a new Adventure for this Franchise?", new_franchise_adventure_path(@franchise) )%>